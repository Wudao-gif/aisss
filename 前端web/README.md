# Brillance - 大学生学习助手

一个基于 AI 的智能学习平台，帮助大学生更高效地学习和复习。

## 🚀 技术栈

- **框架**: Next.js 14 (App Router)
- **语言**: TypeScript
- **UI**: React 19 + Tailwind CSS 4
- **组件库**: Shadcn/ui + Radix UI
- **状态管理**: Zustand
- **图标**: Lucide React
- **包管理**: pnpm

## 📦 安装

```bash
# 安装依赖
pnpm install

# 安装 Zustand（状态管理）
pnpm add zustand

# 开发环境运行
pnpm dev

# 构建生产版本
pnpm build

# 启动生产服务器
pnpm start
```

## 🏗️ 项目结构

```
前端web/
├── app/                    # Next.js App Router 页面
│   ├── page.tsx           # 主页
│   ├── library/           # 图书馆页面
│   └── layout.tsx         # 根布局
├── components/            # React 组件
│   ├── ui/               # UI 基础组件（Shadcn）
│   ├── ErrorBoundary.tsx # 错误边界
│   └── ...               # 业务组件
├── stores/               # Zustand 状态管理
│   ├── useAuthStore.ts   # 认证状态
│   └── useBookshelfStore.ts # 书架状态
├── lib/                  # 工具函数和配置
│   ├── api/             # API 调用层
│   │   ├── client.ts    # HTTP 客户端
│   │   ├── auth.ts      # 认证 API
│   │   └── books.ts     # 图书 API
│   └── utils.ts         # 工具函数
├── types/               # TypeScript 类型定义
│   └── index.ts         # 核心类型
├── hooks/               # 自定义 Hooks
├── public/              # 静态资源
└── styles/              # 全局样式
```

## 🔧 配置说明

### 环境变量

复制 `.env.example` 到 `.env.local` 并配置：

```bash
cp .env.example .env.local
```

主要配置项：
- `NEXT_PUBLIC_API_URL`: 后端 API 地址
- `NEXT_PUBLIC_APP_NAME`: 应用名称
- `NEXT_PUBLIC_ENABLE_WECHAT_LOGIN`: 是否启用微信登录

### TypeScript

项目使用严格的 TypeScript 配置，确保类型安全：

```json
{
  "compilerOptions": {
    "strict": true,
    "noEmit": true
  }
}
```

## 📚 核心功能

### 1. 用户认证
- 邮箱登录/注册
- 微信登录（待开发）
- 用户信息管理

### 2. 图书馆
- 浏览大学教材
- 搜索图书
- 添加到书架

### 3. 学习模式
- 学习模式：基于教材的学习
- 复习模式：复习已学内容
- 解题模式：AI 辅助解题

### 4. 对话系统
- AI 对话助手
- 历史对话管理
- 多轮对话支持

## 🔌 API 集成

### 当前状态
项目目前使用 `localStorage` 模拟后端数据，所有 API 调用都在 `lib/api/` 目录中。

### 对接真实后端

1. **修改 API 基础 URL**
   ```typescript
   // .env.local
   NEXT_PUBLIC_API_URL=https://your-api.com/api
   ```

2. **更新 API 调用**
   ```typescript
   // lib/api/auth.ts
   export async function login(credentials: LoginCredentials) {
     // 替换模拟实现为真实 API 调用
     return post<AuthResponse>('/auth/login', credentials)
   }
   ```

3. **处理认证 Token**
   ```typescript
   // lib/api/client.ts 已经包含 token 处理逻辑
   // 后端返回的 token 会自动添加到请求头
   ```

## 🧪 测试

```bash
# 运行测试（待配置）
pnpm test

# 测试覆盖率
pnpm test:coverage
```

## 📝 开发规范

### 代码风格
- 使用 ESLint + Prettier（待配置）
- 遵循 Airbnb JavaScript Style Guide
- 组件文件使用 PascalCase
- 工具函数使用 camelCase

### Git 提交规范
```
feat: 新功能
fix: 修复 bug
docs: 文档更新
style: 代码格式调整
refactor: 重构
test: 测试相关
chore: 构建/工具链相关
```

### 组件开发规范
1. 每个组件不超过 200 行
2. 复杂逻辑抽离到自定义 Hook
3. 使用 TypeScript 定义 Props
4. 添加必要的注释

## 🚧 待办事项

### 第一阶段：基础架构 ✅
- [x] 修复 TypeScript 配置
- [x] 定义核心类型接口
- [x] 搭建 API 层架构
- [x] 引入状态管理 Zustand
- [x] 添加错误处理机制

### 第二阶段：组件拆分
- [ ] 拆分主页面 page.tsx
- [ ] 拆分 LoginModal 组件
- [ ] 拆分 library 页面
- [ ] 创建共享组件库
- [ ] 优化组件性能

### 第三阶段：工程化配置
- [ ] 配置 ESLint + Prettier
- [ ] 添加测试框架 Vitest
- [ ] 添加 Husky + lint-staged
- [ ] 添加环境变量管理
- [ ] 编写项目文档

## 🤝 贡献指南

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'feat: Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📄 许可证

MIT License

## 👥 团队

- 开发者：吴岛
- 设计：v0.dev

## 📞 联系方式

- Email: 123456@qq.com
- 大学：四川大学

---

**注意**: 本项目目前处于重构阶段，部分功能可能不稳定。

