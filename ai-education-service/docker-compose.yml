services:
  ai-education-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-education-service
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./temp:/app/temp
    restart: unless-stopped

